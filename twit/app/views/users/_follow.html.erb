<%= form_for(current_user.active_relationships.build) do |f|%>
    <div><%= hidden_field_tag :followed_id, @user.id %>
    <div class="followaction"><%= f.submit "Follow", class:"btton"%>
    </div>
<%end%>
