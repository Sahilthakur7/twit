
<h1>All Users</h1>
<%= will_paginate %>
<ul class= "users">
    <% @users.each do |user|%>
        <li>
            <%= image_tag user.avatar.url(:medium)%>
            <%= link_to user.username, user%>
        </li>
    <%end%>
</ul>

<%= will_paginate %>

