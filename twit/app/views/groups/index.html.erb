<ul class="actions">
    <h4><li><%= link_to "Create Group", new_group_path %></li></h4>     
</ul>


<h1>All Groups</h1>
<%= will_paginate %>

<ul class= "groups">
    <% @groups.each do |group|%>
        <li>
            <%= link_to group.name, group_path(group) %>
        </li>
    <%end%>
</ul>

<%= will_paginate %>

