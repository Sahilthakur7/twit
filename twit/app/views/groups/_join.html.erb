<%= form_for(current_user.active_groups.build, remote: true) do |f|%>
    <div><%= hidden_field_tag :joined_id, @group.id %>
    <div class="followaction"><%= f.submit "Join", class:"btton"%>
    </div>
<%end%>
