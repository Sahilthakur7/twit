<%= form_for(current_user.group_relationships.build, remote: true) do |f|%>
    <div><%= hidden_field_tag :group_id, @group.id %>
    <div class="followaction"><%= f.submit "Join", class:"btton"%>
    </div>
<%end%>
